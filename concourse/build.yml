---    
platform: linux
image: "docker:///java#8"
inputs:
  - name: weatherbus-source
outputs:
  - name: weatherbus-artifacts
    path: weatherbus-source/artifacts
run:
  path: "weatherbus-source/gradlew"
  args: ["build", "-pweatherbus-source"]
params:
  "TERM": "xterm"

